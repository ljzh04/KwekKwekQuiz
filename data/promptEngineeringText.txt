{
  "description": "Generate a comprehensive quiz covering all key points in the specified topic. The quiz should include at least 20 questions across multiple formats.",
  "structure": {
    "question": {
      "type": "string",
      "description": "Markdown-formatted question text (supports headers, bold/italic, code blocks)",
      "example": "### What does this Python code do?\n```python\ndef add(a, b):\n    return a + b\n```"
    },
    "options": {
      "type": "array",
      "description": "Only required for multiple-choice questions. Each option should be a Markdown-formatted string.",
      "example": ["Subtracts", "Adds", "Divides", "Multiplies"]
    },
    "correct": {
      "type": "any",
      "description": "For multiple-choice: index (0-based). For true/false: boolean. For other types: string matching expected answer.",
      "example": 1
    }
  },
  "requirements": [
    "Include at least 20 questions covering all key points in the topic",
    "Override minimum question number if explicitly defined by the user prompt",
    "Ensure all Markdown is properly escaped for JSON serialization",
    "Maintain consistent formatting across all question types",
    "Avoid ambiguous or poorly defined questions"
  ],
  "example": [
    {
      "type": "multiple-choice",
      "question": "### What does this Python code do?\n```python\ndef add(a, b):\n    return a + b\n```",
      "options": ["Subtracts", "Adds", "Divides", "Multiplies"],
      "correct": 1
    },
    {
      "type": "true-false",
      "question": "**The Sun rises in the East.**",
      "correct": true
    },
    {
      "type": "fill-in-the-blank",
      "question": "The chemical symbol for **water** is H<sub>2</sub>_.",
      "correct": "O"
    }
  ]
}
